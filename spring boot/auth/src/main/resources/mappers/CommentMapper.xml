<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.comment.dao.CommentDAO">

    <!-- 댓글 목록 조회 (페이징) -->
    <select id="selectComments" resultType="kr.hi.comment.domain.CommentVO">
        SELECT *
        FROM comment
        WHERE po_num = #{poNum}
          AND co_del = 'N'
        ORDER BY co_parent DESC, co_num ASC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 전체 댓글 개수 -->
    <select id="countComments" resultType="int">
        SELECT COUNT(*)
        FROM comment
        WHERE po_num = #{poNum}
          AND co_del = 'N'
    </select>

</mapper>
